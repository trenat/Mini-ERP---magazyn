<UserControl x:Class="MiniERP_desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniERP_desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <DockPanel HorizontalAlignment="Center">
                <Label FontSize="15" Content="Witaj"/>
                <Label FontSize="15" x:Name="User_FirstName"/>
                <Label FontSize="15" Content="!"></Label>
            </DockPanel>
            <DockPanel HorizontalAlignment="Center">
                <Label FontSize="15" Content="Zarządzaj swoim magazynem"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
