<UserControl x:Class="MiniERP_desktop.Views.NewItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniERP_desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
          <StackPanel Margin="10">
              <StackPanel.Resources>
                  <Style TargetType="Label">
                      <Setter Property="Width" Value="120"></Setter>
                  </Style>
              </StackPanel.Resources>
              <DockPanel>
                  <Label Content="Name"/>
                  <TextBox x:Name="Item_Name"/>
            </DockPanel>
              <DockPanel>
                  <Label Content="Description"/>
                  <TextBox x:Name="Item_Description"/>
            </DockPanel>
              <DockPanel>
                  <Label Content="Price"/>
                  <TextBox x:Name="Item_Price"/>
            </DockPanel>
            <DockPanel Margin="0,10,0,0">
                <Button Content="Add" x:Name="AddItem"/>
              </DockPanel>
        </StackPanel>  
    </Grid>
</UserControl>
