<UserControl x:Class="MiniERP_desktop.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniERP_desktop.Views"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:ex="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Top" Width="210" Height="297" Margin="18,8,24,14">
        <Grid.Effect>
            <DropShadowEffect/>
        </Grid.Effect>
        <ex:RichTextBox IsReadOnly="True">
            <RichTextBox.Document>
                <FlowDocument Background="{Binding BackColor}" Foreground="{Binding TextColor}" AllowDrop="False" >
                    <Section>
                        <Paragraph FontSize="18" Block.TextAlignment="Center">
                            <LineBreak/>
                            Invoice VAT
                            
                        </Paragraph>
                    </Section>
                </FlowDocument>
                <!--<Paragraph />-->
            </RichTextBox.Document>
            </ex:RichTextBox>
    </Grid>
</UserControl>
