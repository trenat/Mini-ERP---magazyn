<UserControl x:Class="MiniERP_desktop.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniERP_desktop.Views"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <DockPanel Dock="Top">
            <Label DockPanel.Dock="Left">Typ dokumentu: </Label>
            <ComboBox x:Name="DocumentType" DockPanel.Dock="Right"/>
        </DockPanel>
        <DockPanel>
            <ContentControl x:Name="Content" DockPanel.Dock="Top"/>

            <Button DockPanel.Dock="Bottom" Content="Generuj" VerticalAlignment="Top" Width="75"/>
        </DockPanel>   
    </DockPanel>
</UserControl>
